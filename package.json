{
  "scripts": {
    "emulators-on": "firebase emulators:start --inspect-functions --import=./local_emulator --export-on-exit"
  },
  "dependencies": {
    "chance": "^1.1.8",
    "firebase-functions": "^3.15.7",
    "html2canvas": "^1.3.2"
  }
}
